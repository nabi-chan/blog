export default function Loading() {
  return (
    <main className="site-shell py-24">
      <section className="memo-card mx-auto max-w-xl text-center">
        <span className="sticker sticker-mint mx-auto">LOADING</span>
        <p className="mt-6 text-3xl text-(--ink)">스티커를 붙이는 중...</p>
      </section>
    </main>
  );
}
