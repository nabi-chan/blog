{
  "name": "@nabi-chan/blog-repo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build --env-mode=loose",
    "dev": "turbo dev",
    "format": "prettier --write .",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-packagejson": "^2.5.1",
    "turbo": "^2.0.10",
    "typescript": "^5.5.4"
  },
  "packageManager": "bun@1.1.12",
  "engines": {
    "node": "20.16.0"
  },
  "trustedDependencies": [
    "@vercel/speed-insights"
  ]
}
